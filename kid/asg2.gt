proc main(val int c, val float d)
    int a;
    int b;
begin
    a := 2 * (1 - 10) + 2 + 2 / 2 + 14 * 5.4 - i;
    if a >= 1 && a <= 3 || a > 5 then
        a := 3;
    fi
    while n > 1 do
        call p(n);
    od
    write a;
    write "\n";
    b := -a + a;
    write b;
    write "\n";
end

proc p(val float n)

begin
    write n;
end