proc p(val int x, ref float y)
    int d;
    int a;
    bool f;
begin
    write "KK";
    write "\n";
end

proc main()
    int a[4,4];
    float b;
    bool c;
begin
    # a[1,1] := 1;
    # call p(1,a[0,0]);
    read b;
    c := (2.3 > 1) && (1 < 4);
    call p(1,b);
    write "Hello, World!\n";
end